package com.fintrack.api.shared.config;

/**
 * Configuração de auditoria do JPA — delegada para JpaConfig.
 */
public class AuditConfig {
    // @EnableJpaAuditing configurado em JpaConfig com auditorAwareRef
}
