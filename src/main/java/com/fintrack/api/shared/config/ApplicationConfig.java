package com.fintrack.api.shared.config;

/**
 * Classe mantida por compatibilidade. Configurações de segurança foram movidas para SecurityConfig.
 */
public class ApplicationConfig {
    // Intencionalmente vazia — beans migrados para SecurityConfig e UserDetailsServiceImpl
}
