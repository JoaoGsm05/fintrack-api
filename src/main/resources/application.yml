spring:
  application:
    name: fintrack-api
  profiles:
    active: dev
  jpa:
    open-in-view: false

server:
  port: 8080

app:
  security:
    jwt:
      secret: ${JWT_SECRET:change-me-in-production-use-256bit-key}
      expiration: 900000        # 15 minutos
      refresh-expiration: 604800000  # 7 dias
