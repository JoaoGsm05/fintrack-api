# Lombok config — gerado automaticamente
# Impede que o Lombok interfira em records Java 21
# (problema conhecido: processor strip canonical constructor de records sem anotações Lombok)
lombok.addLombokGeneratedAnnotation = true
